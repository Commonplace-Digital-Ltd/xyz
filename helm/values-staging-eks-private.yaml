# These values apply to the staging environment, and overrides config in values.yaml
# as needed.
#
# This values file is applied on each deployment to the environment by AWS Codepipeline.
#
env:
  XYZ_ENV: staging
  MONGODB_URL: mongodb+srv://cp-non-prod-pl-0.lfxwi.mongodb.net?retryWrites=true&w=3&readPreference=nearest
  DBS_XYZ: postgres://cp-staging.ct36hfcslmg5.eu-west-2.rds.amazonaws.com/map

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 200
  targetMemoryUtilizationPercentage: 200
